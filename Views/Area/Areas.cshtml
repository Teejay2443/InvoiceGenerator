@using InvoiceGenerator.Dto;
@using InvoiceGenerator.Dto;
@model IEnumerable<AreaCoveredDto>;
@{
	ViewData["Title"] = "Areas";
	var i = 0;
	var totalAreas = Model.Count();

}

<div class="container mt-4">
	<h2 class="mb-4">Total Service Area Available: <span class="badge bg-primary">@totalAreas</span></h2>
	<a asp-controller="Area" asp-action="CreateArea" class="btn btn-info btn-sm">Create New Service Area </a>

	<table class="table table-striped table-bordered">
		<thead class="table-light">
			<tr>
				<th scope="col">#</th>
				<th scope="col">Name</th>
				<th scope="col" class="text-center">Actions</th>

			</tr>
		</thead>
		<tbody>
			@if (Model.Any())
			{
				foreach (var item in Model)
				{
					i++;
					<tr>
						<th scope="row">@i</th>
						<td>@item.Name</td>
						<td class="text-center">
							<a asp-controller="Area" asp-action="AreaDetail" asp-route-id="@item.Id" class="btn btn-sm btn-success">
								<i class="fas fa-edit"></i> Details
							</a>
							<a asp-controller="Area" asp-action="DeleteArea" asp-route-id="@item.Id" class="btn btn-danger btn-sm">Delete</a>
						</td>
					</tr>
				}
			}
			else
			{
				<tr>
					<td colspan="7" class="text-center">
						<h5>No Service Area  Created Yet.</h5>

					</td>
				</tr>
			}
		</tbody>
	</table>

	<div class="text-end">
	</div>
</div>

<a asp-controller="Home" asp-action="Index" class="btn btn-primary btn-sm">Back To Home </a>

