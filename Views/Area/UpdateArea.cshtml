@using InvoiceGenerator.Dto;
  @using InvoiceGenerator.Dto;
@model AreaCoveredDto;
@{
    ViewData["Title"] = "Update  Service Area";
    var Id = Context.GetRouteValue(" Id");
}

<div class="col-md-8">
    <form asp-controller="Area" asp-action="UpdateArea">
        <div class="mb-3">
            <label class="form-label">Service Area</label>
            <input type="text" class="form-control" name="Name" placeholder="Name" required value="@Model.Name">
        </div>

        <div class="row">
            <button type="submit" class="btn btn-primary">Update</button>
        </div>
    </form>
</div>

